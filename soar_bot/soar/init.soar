sp {ants*propose-init
(state <s> ^superstate nil -^name)
-->
(<s> ^operator <o> +)
(<o> ^name init)}

sp {ants*apply-init
(state <s> ^operator <o>)
(<o> ^name init)
-->
(<s> ^name ants ^my-id <my-id>)
(<my-id> ^string (concat 0) ^int (int 0))}
