sp {ants*propose*done
(state <s> ^name ants ^turn <t>)
-->
(<s> ^operator <o> + =)
(<o> ^name done)}

sp {ants*compare*done-move
(state <s> ^name ants ^operator <od> + ^operator <om> +)
(<od> ^name done)
(<om> ^name move)
-->
(<s> ^operator <od> -)}

sp {ants*apply*done
(state <s> ^name ants ^operator <o> ^io.output-link <ol>)
(<o> ^name done)
-->
(<ol> ^done <d>)}
